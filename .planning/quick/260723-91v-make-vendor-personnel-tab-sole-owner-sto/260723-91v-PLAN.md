---
phase: 260723-91v-make-vendor-personnel-tab-sole-owner-sto
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/ContractManagementPage/components/EditContract.tsx
  - src/pages/MsaPage/layouts/CreateMSADialog.tsx
autonomous: true
requirements: [personnel-tab-sole-owner]

must_haves:
  truths:
    - "Editing a contract via EditContract.tsx no longer sends a personnel field in its PUT payload"
    - "Editing an MSA via CreateMSADialog.tsx no longer sends a personnel field in its PUT payload"
    - "Creating an MSA via CreateMSADialog.tsx (isEditing=false) still sends personnel exactly as before"
    - "Creating a contract via CreateContractSheet is untouched (out of scope, no changes made)"
  artifacts:
    - path: "src/pages/ContractManagementPage/components/EditContract.tsx"
      provides: "buildPayload with personnel field and its computation removed; unused toPersonnelOrUndefined import removed if orphaned"
    - path: "src/pages/MsaPage/layouts/CreateMSADialog.tsx"
      provides: "buildPayload with personnel conditionally excluded from payload only when isEditing is true"
  key_links:
    - from: "EditContract.tsx buildPayload"
      to: "PUT /manager/contracts/{id}"
      via: "payload object built and passed to mutationFn"
      pattern: "personnel"
    - from: "CreateMSADialog.tsx buildPayload"
      to: "PUT /contract/manager/msa-contracts/{editingMsaId} vs POST /contract/manager/msa-contracts"
      via: "isEditing branch in createMutation.mutationFn"
      pattern: "isEditing"
---

<objective>
Stop the EditContract edit-PUT and the CreateMSADialog edit-PUT from sending a `personnel` field, so the dedicated Vendor Personnel tab (`GET`/`PUT .../vendor-personnel`, shipped in 6cd5433f2) remains the sole authoritative writer of vendor personnel. Contract/MSA CREATE flows must keep sending `personnel` unchanged — there is no contract/MSA id yet at creation time for the dedicated endpoint.

Purpose: Prevent the stale Step-2 personnel chips array from clobbering personnel data the Vendor Personnel tab already saved, whenever a user edits a contract or MSA through the monolithic edit form.
Output: Two files modified — EditContract.tsx (personnel fully removed from its edit-only payload) and CreateMSADialog.tsx (personnel removed only on the isEditing branch, preserved on create).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

Relevant source (already read during planning):
- src/pages/ContractManagementPage/components/EditContract.tsx (lines ~880-1012): `buildPayload` computes `personnel` at lines 919-926 from `data.personnelMeta`/`data.personnel`, maps through `toPersonnelOrUndefined`, and includes it in the returned `payload` object at line 984 as `personnel,`. This component is edit-only (no create path lives here — `EditContract.tsx` is only rendered for editing an existing contract). The payload is later stripped of `undefined` keys via the `Object.keys(payload).forEach(...)` loop at lines 997-1001, then PUT to `/manager/contracts/{id}`.
- src/pages/MsaPage/layouts/CreateMSADialog.tsx (lines ~760-1021): Single shared `buildPayload` used for BOTH create and edit. `isEditing` (line 217) is `Boolean(editingMsaId)` and is in scope inside `buildPayload`'s closure (used elsewhere in the file, e.g. lines 306, 610). `personnel` is computed at lines 802-806 from `data.personnel` via `toPersonnelOrUndefined`, then included in `payload` at line 953 as `personnel,`. The returned payload is passed through `pruneEmptyValuesDeep(payload)` (line 965) before being handed to `createMutation`, whose `mutationFn` (lines 977-990) branches on `isEditing && editingMsaId` to PUT `/contract/manager/msa-contracts/${editingMsaId}` vs POST `/contract/manager/msa-contracts`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stop EditContract's edit PUT from sending personnel</name>
  <files>src/pages/ContractManagementPage/components/EditContract.tsx</files>
  <action>
  In `buildPayload`, remove the `personnel` computation block (the `const personnel = ...` statement built from `data.personnelMeta`/`data.personnel` via `toPersonnelOrUndefined`) and remove the `personnel,` line from the returned `payload` object. This component only ever renders in edit mode, so no create-path carve-out is needed here — personnel must simply never be part of this component's PUT payload.

  After removing the computation, check whether `toPersonnelOrUndefined` (imported at the top of the file) is still referenced anywhere else in EditContract.tsx. If it is now orphaned, remove it from the import statement — do not leave an unused import. Do not touch any other import or unrelated code in this file.

  Do not modify the Step 2 personnel chips UI/state (`data.personnel`, `data.personnelMeta`, the chip-editing form fields) — those still render during edit; only the submitted payload changes. This is a deliberate, documented gap (see Success Criteria) for a later task to address separately.
  </action>
  <verify>
    <automated>npx tsc -b</automated>
  </verify>
  <done>buildPayload's returned object has no `personnel` key; `toPersonnelOrUndefined` import is removed if it became unused (or confirmed still used elsewhere and left in place); `npx tsc -b` is clean.</done>
</task>

<task type="auto">
  <name>Task 2: Stop CreateMSADialog's edit PUT from sending personnel, keep it on create</name>
  <files>src/pages/MsaPage/layouts/CreateMSADialog.tsx</files>
  <action>
  In `buildPayload`, change the `personnel` key in the returned `payload` object so it is only included when NOT editing. Since `isEditing` is already in scope inside `buildPayload`, change the `personnel,` line (currently just referencing the `personnel` const built at lines 802-806) to conditionally resolve to `undefined` when `isEditing` is true, e.g. `personnel: isEditing ? undefined : personnel,`. Leave the `const personnel = ...` computation itself untouched (it's still needed for the create path) — only gate its use in the final payload object.

  Confirm `pruneEmptyValuesDeep` (applied to the payload before return, line 965) strips `undefined` values so the key does not appear at all in the edit PUT body — read the helper's implementation if not already confirmed during planning to verify this behavior before relying on it silently.

  Do not change the `internalTeam` field or any other payload key. Do not change the create branch's behavior — on create (`isEditing` false), `personnel` must be identical to current behavior.
  </action>
  <verify>
    <automated>npx tsc -b</automated>
  </verify>
  <done>When `isEditing` is true, the payload sent to `PUT /contract/manager/msa-contracts/{editingMsaId}` has no `personnel` key (verified via `pruneEmptyValuesDeep` stripping the `undefined` value, or explicit key deletion if the helper does not strip it). When `isEditing` is false, payload still includes `personnel` exactly as before. `npx tsc -b` is clean.</done>
</task>

</tasks>

<verification>
- `npx tsc -b` passes with zero errors.
- Grep confirms no remaining `personnel` key assignment in EditContract.tsx's `buildPayload` return object.
- Grep confirms CreateMSADialog.tsx's `personnel` payload key is gated on `isEditing`.
- No changes made to CreateContractSheet or CreateMSADialog's create-only code paths.
</verification>

<success_criteria>
- EditContract.tsx PUT to `/manager/contracts/{id}` never includes a `personnel` field.
- CreateMSADialog.tsx PUT to `/contract/manager/msa-contracts/{editingMsaId}` never includes a `personnel` field.
- CreateMSADialog.tsx POST to `/contract/manager/msa-contracts` (create) still includes `personnel` unchanged.
- CreateContractSheet (contract create flow) is unmodified and still sends `personnel`.
- Deferred follow-up (NOT implemented in this plan, noted for a future task): Step 2 personnel chips in both EditContract.tsx and CreateMSADialog.tsx (edit mode) still render and are editable in the UI, but changes made there are now silently discarded on save. A later task should make these chips read-only or hidden during edit and redirect users to the dedicated Vendor Personnel tab.
</success_criteria>

<output>
After completion, create `.planning/quick/260723-91v-make-vendor-personnel-tab-sole-owner-sto/260723-91v-SUMMARY.md`
</output>
