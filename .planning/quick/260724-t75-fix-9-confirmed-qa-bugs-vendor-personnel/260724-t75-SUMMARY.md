---
phase: 260724-t75-fix-9-confirmed-qa-bugs-vendor-personnel
plan: 01
subsystem: ui
tags: [react, tanstack-query, vitest, dashboard, navigation, ai-chat, redline, admin]

# Dependency graph
requires: []
provides:
  - Vendor Personnel save invalidates the parent contract/MSA detail query (invalidateQueryKey prop)
  - AI redline auto-run gated by negotiation turn (redlineTurn.canAct)
  - Admin row-level delete corrected to /delete/{adminId}
  - transformSubDistribution emits a real percentage field; donut legend renders it
  - ChartCard getDataKeys excludes the percentage companion field from plotted bar series
  - Company Admin Solicitation Status chart legend left-aligned
  - Company Admin Contracts tab wired to real contract data (isContractDashboardRole, companyAdminContractsRows)
  - Company Admin nav reordered (Evaluation Management after Solicitation Management) and pruned (Contract Management removed)
  - AI chat widget suggested prompts are role-aware (company_admin gets company-wide prompts)
affects: [dashboard, navigation, ai-chat, collaboration-tool, admin-management, contract-management, msa]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Optional invalidateQueryKey prop pattern for child components to invalidate a parent's TanStack Query cache on mutation success"
    - "percentage companion field alongside value in chart transformer output, excluded from plotted series by field-name filter"

key-files:
  created: []
  modified:
    - src/pages/ContractManagementPage/layouts/VendorPersonnelTabContent.tsx
    - src/pages/ContractManagementPage/ContractDetailPage.tsx
    - src/pages/MsaPage/MsaDetailPage.tsx
    - src/pages/ContractManagementPage/__tests__/vendor-personnel-tab.test.tsx
    - src/pages/CollaborationToolPage/index.tsx
    - src/pages/AdminManagementPage/index.tsx
    - src/lib/dashboardDataTransformer.ts
    - src/components/layouts/RoleBasedDashboard/components/ChartCard.tsx
    - src/config/dashboardConfig.ts
    - src/lib/__tests__/dashboardDataTransformer.test.ts
    - src/hooks/useDashboardData.ts
    - src/components/layouts/RoleBasedDashboard/index.tsx
    - src/hooks/__tests__/useDashboardData.test.tsx
    - src/lib/navigation.ts
    - src/components/layouts/AIChatWidget/index.tsx
    - src/lib/__tests__/navigation.test.ts

key-decisions:
  - "Task 4: extended the AI-insights query's own standalone `enabled` role list (separate from isContractDashboardRole) to include company_admin — required to satisfy the plan's own <behavior> spec for that query, not just the isContractDashboardRole toggle"
  - "Task 4: companyAdminContractsRows sources contract-shaped activity rows from dashboardConfigs.contract_manager.rows (not enhancedDashboardConfig.rows) since company_admin's own base config has no activity-type row to reuse — per plan's <interfaces> guidance"

patterns-established:
  - "invalidateQueryKey optional prop for cross-boundary cache invalidation (already precedented by AssignProjectManagerDialog, now also used by VendorPersonnelTabContent)"

requirements-completed: [QA-253, QA-256, QA-258, QA-259, QA-260, QA-261, QA-262, QA-263, QA-264, QA-265, QA-266, QA-269]

# Metrics
duration: 19min
completed: 2026-07-24
---

# Quick Task 260724-t75: Fix 9 Confirmed QA Bugs Summary

**Fixed 9 confirmed QA bugs across vendor personnel refresh, AI redline turn-gating, admin delete endpoint, dashboard chart data/legend bugs, Company Admin contract data wiring, and role-aware nav/AI-chat prompts — all frontend-only, no BE changes.**

## Performance

- **Duration:** 19 min
- **Started:** 2026-07-24T20:48:08Z
- **Completed:** 2026-07-24T21:07:10Z
- **Tasks:** 5
- **Files modified:** 16

## Accomplishments
- Vendor personnel add/edit/remove now refreshes the parent Contract/MSA detail view immediately (no manual reload) via a new `invalidateQueryKey` prop
- AI redline suggestions no longer auto-generate on the opposing side's negotiation turn; admin row-level delete now hits the correct endpoint
- Subscription Distribution donut legend shows a real percentage (not raw count mislabeled with "%"); Role Distribution bar chart no longer renders a stray second "percentage" bar series; Company Admin's Solicitation Status legend is left-aligned
- Company Admin's Contracts Overview/Analytics tab now shows real contract data (stats, cycle time, invoice status, risk) instead of solicitation data
- Company Admin's left nav shows Evaluation Management right after Solicitation Management with Contract Management removed; AI chat widget shows company-admin-appropriate suggested prompts

## Task Commits

Each task was committed atomically:

1. **Task 1: Vendor Personnel save invalidates parent contract/MSA detail query (#253)** - `2607d33f8` (feat)
2. **Task 2: Redline auto-run turn guard (#256) + Admin delete endpoint fix (#258)** - `cb7ee3e96` (fix)
3. **Task 3: Dashboard chart fixes — real percentage, drop stray bar series, left-align legend (#259/#260, #261, #264)** - `6e6a25655` (fix)
4. **Task 4: Company Admin Contracts tab shows real contract data (#262/#263)** - `c4e7f1b19` (fix)
5. **Task 5: Company Admin nav reorder/prune (#265/#266) + role-aware AI chat prompts (#269)** - `c521d31f3` (feat)

_Note: each TDD task's test coverage and implementation were committed together as a single atomic task commit (this is a quick-task plan, type `execute` — not a plan-level `type: tdd` gate), matching how the plan's own `<action>` steps interleaved test-writing with implementation per task._

## Files Created/Modified
- `src/pages/ContractManagementPage/layouts/VendorPersonnelTabContent.tsx` - Added optional `invalidateQueryKey` prop, invalidated in `saveMutation.onSuccess` alongside the tab's own queryKey
- `src/pages/ContractManagementPage/ContractDetailPage.tsx` - Wired `invalidateQueryKey={[...queryKey]}` to `VendorPersonnelTabContent`
- `src/pages/MsaPage/MsaDetailPage.tsx` - Wired `invalidateQueryKey={[...queryKey]}` to `VendorPersonnelTabContent`
- `src/pages/ContractManagementPage/__tests__/vendor-personnel-tab.test.tsx` - New `invalidateQueryKey` test coverage (with and without the prop)
- `src/pages/CollaborationToolPage/index.tsx` - Auto-run Redline effect now returns early when `!redlineTurn.canAct`
- `src/pages/AdminManagementPage/index.tsx` - `deleteAdmin` mutation URL corrected from `/admins/{adminId}` to `/delete/{adminId}`
- `src/lib/dashboardDataTransformer.ts` - `transformSubDistribution` now computes and returns a `percentage` field
- `src/components/layouts/RoleBasedDashboard/components/ChartCard.tsx` - Donut legend formatter prefers `item.percentage`; `getDataKeys` excludes `"percentage"` from plotted bar series
- `src/config/dashboardConfig.ts` - Company Admin's `solicitation-status` chart config gets `legendPosition: { horizontal: "left" }`
- `src/lib/__tests__/dashboardDataTransformer.test.ts` - New `transformSubDistribution` describe block (real percentages + zeroed fallback)
- `src/hooks/useDashboardData.ts` - `isContractDashboardRole` includes `company_admin`; AI-insights query's own `enabled` list extended to include `company_admin`
- `src/components/layouts/RoleBasedDashboard/index.tsx` - New `companyAdminContractsRows` memo; company_admin's Contracts tab now passes `cmTotalStats`/`companyAdminContractsRows` instead of `enhancedDashboardConfig`
- `src/hooks/__tests__/useDashboardData.test.tsx` - New company_admin AI-insights test mirroring procurement/approver
- `src/lib/navigation.ts` - company_admin nav: Evaluation Management moved after Solicitation Management (renamed from "Evaluation"), Contract Management block removed
- `src/components/layouts/AIChatWidget/index.tsx` - Suggested prompts are role-aware via `useUserRole()`; new `COMPANY_ADMIN_SUGGESTED_PROMPTS`
- `src/lib/__tests__/navigation.test.ts` - New test asserting Evaluation Management placement and Contract Management absence for company_admin

## Decisions Made
- Extended the AI-insights query's standalone `enabled` role check (separate from `isContractDashboardRole`) to include `company_admin` — the plan's Task 4 `<behavior>` spec required this specific query to fire for company_admin, and this query uses its own explicit role list rather than `isContractDashboardRole`.
- Sourced `companyAdminContractsRows` from `dashboardConfigs.contract_manager.rows` (not `enhancedDashboardConfig.rows`) per the plan's `<interfaces>` note that company_admin's own base config has no `"activity"`-type row to reuse.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Extended AI-insights query's enabled list for company_admin (Task 4)**
- **Found during:** Task 4 (Company Admin Contracts tab real contract data)
- **Issue:** The plan's action step 1 only mentioned adding `company_admin` to `isContractDashboardRole`, but the `contractManagerAiInsights` query (line ~1285) has its own separate `enabled` role check (`userRole === "contract_manager" || userRole === "procurement" || userRole === "approver"`) not gated by `isContractDashboardRole`. Without extending it, the plan's own `<behavior>` requirement ("useDashboardData('company_admin') requests AI insights...") and the corresponding TDD test would fail.
- **Fix:** Added `|| userRole === "company_admin"` to that query's `enabled` condition.
- **Files modified:** src/hooks/useDashboardData.ts
- **Verification:** New "requests AI insights for company_admin" test passes alongside existing procurement/approver tests.
- **Committed in:** c4e7f1b19 (Task 4 commit)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Necessary to satisfy the plan's own stated behavior/test requirement for Task 4. No scope creep — same file, same task, same QA item (#262).

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- All 9 QA items (#253, #256, #258, #259/#260, #261, #262/#263, #264, #265/#266, #269) closed out; ready to ship in the next PR to `fix/phase2-bug-fixes`.
- No blockers introduced. Server-side authorization for the newly-enabled `/contract/manager/contracts/dashboard/*` requests (company_admin) is unchanged by this FE-only fix — if the BE doesn't already permit company_admin to read this data it will 403 server-side (per plan's threat model, disposition: accept).

---
*Phase: 260724-t75-fix-9-confirmed-qa-bugs-vendor-personnel*
*Completed: 2026-07-24*

## Self-Check: PASSED

- All 16 files listed in the plan's `files_modified` frontmatter exist and match `git diff --name-only fe2d420ae..HEAD` exactly.
- All 5 task commit hashes (2607d33f8, cb7ee3e96, 6e6a25655, c4e7f1b19, c521d31f3) verified present in `git log --oneline --all`.
- Scoped vitest run across all 4 test files: 20/20 tests passed.
- `npx tsc -b`: no errors.
