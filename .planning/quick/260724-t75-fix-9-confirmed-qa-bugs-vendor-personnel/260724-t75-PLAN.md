---
phase: 260724-t75-fix-9-confirmed-qa-bugs-vendor-personnel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/ContractManagementPage/layouts/VendorPersonnelTabContent.tsx
  - src/pages/ContractManagementPage/ContractDetailPage.tsx
  - src/pages/MsaPage/MsaDetailPage.tsx
  - src/pages/ContractManagementPage/__tests__/vendor-personnel-tab.test.tsx
  - src/pages/CollaborationToolPage/index.tsx
  - src/pages/AdminManagementPage/index.tsx
  - src/lib/dashboardDataTransformer.ts
  - src/components/layouts/RoleBasedDashboard/components/ChartCard.tsx
  - src/config/dashboardConfig.ts
  - src/lib/__tests__/dashboardDataTransformer.test.ts
  - src/hooks/useDashboardData.ts
  - src/components/layouts/RoleBasedDashboard/index.tsx
  - src/hooks/__tests__/useDashboardData.test.tsx
  - src/lib/navigation.ts
  - src/components/layouts/AIChatWidget/index.tsx
  - src/lib/__tests__/navigation.test.ts
autonomous: true
requirements: [QA-253, QA-256, QA-258, QA-259, QA-260, QA-261, QA-262, QA-263, QA-264, QA-265, QA-266, QA-269]

must_haves:
  truths:
    - "Adding/editing/removing vendor personnel immediately refreshes the parent Contract/MSA detail view (Vendor/Contractor Key Personnel section) without a manual page reload"
    - "AI redline suggestions do not auto-generate when it isn't the current user's negotiation turn; they still auto-generate normally when it is the user's turn"
    - "Deleting an admin from the Admin Management list row action succeeds by hitting the same correct endpoint the details-sheet delete already uses"
    - "The Subscription Distribution donut legend shows a real percentage of the whole (segments sum to ~100%), not the raw subscriber count mislabeled with a '%' suffix"
    - "The Role Distribution bar chart renders exactly one bar series per role — no second, unlabeled, randomly-colored 'percentage' bar"
    - "Company Admin's Contracts Overview/Analytics tab shows real contract data (stats, cycle time, invoice status, risk, etc.), not solicitation data"
    - "The Solicitation Status pie chart legend on the Company Admin dashboard is left-aligned, not centered"
    - "Company Admin's left nav shows 'Evaluation Management' immediately after 'Solicitation Management', and no longer shows a 'Contract Management' entry"
    - "The AI chat widget shows company-admin-appropriate suggested prompts (company-wide user/vendor/subscription topics) instead of the per-contract prompt set when a company_admin is logged in"
  artifacts:
    - path: "src/pages/ContractManagementPage/layouts/VendorPersonnelTabContent.tsx"
      provides: "Optional invalidateQueryKey prop invalidated alongside the tab's own queryKey on saveMutation success"
    - path: "src/pages/CollaborationToolPage/index.tsx"
      provides: "redlineTurn.canAct guard on the Redline-tab auto-run effect"
    - path: "src/pages/AdminManagementPage/index.tsx"
      provides: "Row-level admin delete action calling /delete/{adminId}"
    - path: "src/lib/dashboardDataTransformer.ts"
      provides: "transformSubDistribution emits a computed percentage field alongside value"
    - path: "src/components/layouts/RoleBasedDashboard/components/ChartCard.tsx"
      provides: "Donut legend renders percentage (not raw value); getDataKeys excludes the display-only percentage field from rendered bar series"
    - path: "src/config/dashboardConfig.ts"
      provides: "Company Admin Solicitation Status chart config with legendPosition left-aligned"
    - path: "src/hooks/useDashboardData.ts"
      provides: "isContractDashboardRole includes company_admin so contractManagerXXX queries are enabled for that role"
    - path: "src/components/layouts/RoleBasedDashboard/index.tsx"
      provides: "companyAdminContractsRows + cmTotalStats wired into company_admin's Contracts tab ContractsTabView"
    - path: "src/lib/navigation.ts"
      provides: "company_admin nav array with Evaluation Management repositioned after Solicitation Management and Contract Management removed"
    - path: "src/components/layouts/AIChatWidget/index.tsx"
      provides: "Role-aware suggested prompts via useUserRole()"
  key_links:
    - from: "VendorPersonnelTabContent saveMutation.onSuccess"
      to: "qc.invalidateQueries({ queryKey: invalidateQueryKey })"
      via: "optional invalidateQueryKey prop passed from ContractDetailPage.tsx and MsaDetailPage.tsx"
      pattern: "invalidateQueryKey"
    - from: "CollaborationToolPage auto-run useEffect (activeTab === \"redline\")"
      to: "redlineTurn.canAct"
      via: "early return guard before runAiSuggestions()"
      pattern: "redlineTurn.canAct"
    - from: "dashboardDataTransformer.transformSubDistribution"
      to: "ChartCard.tsx donut Legend formatter"
      via: "item.percentage field read in place of item.value"
      pattern: "item.percentage"
    - from: "useDashboardData.isContractDashboardRole"
      to: "RoleBasedDashboard company_admin Contracts tab branch"
      via: "contractManagerXXX queries enabled=true for company_admin feed cmTotalStats/companyAdminContractsRows"
      pattern: "isContractDashboardRole"
---

<objective>
Fix 9 confirmed QA bugs from the SwiftPro review doc, all root-caused via direct code investigation this session (file:line evidence already gathered — do not re-investigate): #253 (vendor personnel save doesn't refresh contract detail), #256 (AI redline auto-generates regardless of negotiation turn), #258 (admin-removal list action calls wrong endpoint), #259/#260 (subscription-distribution legend shows raw count mislabeled as a percentage), #261 (role-distribution chart renders an unwanted second "percentage" bar series), #262/#263 (Company Admin's Contracts Overview/Analytics show solicitation data instead of contract data), #264 (solicitation-category legend not left-aligned), #265/#266 (Company Admin nav needs reordering + pruning), #269 (AI chat suggested prompts aren't role-aware).

Purpose: Close out this QA batch (frontend-only, Vite/React) so the fixes ship in the next PR to `fix/phase2-bug-fixes`.

Output: 5 tasks, each touching a distinct file cluster, delivering all 9 fixes with no scope creep beyond what's listed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

This is a frontend-only repo (Vite/React) — no backend code to write. Every fix below is a targeted, surgical change; do not touch anything outside what's specified per task.

<interfaces>
Precedent for the invalidateQueryKey pattern (Task 1) — from `src/pages/ContractManagementPage/components/AssignProjectManagerDialog.tsx`:

```typescript
invalidateQueryKey: unknown[]; // prop type
// usage at call site (ContractDetailPage.tsx:644):
<AssignProjectManagerDialog invalidateQueryKey={[...queryKey]} ... />
// usage inside the dialog's own mutation onSuccess:
await queryClient.invalidateQueries({ queryKey: invalidateQueryKey });
```

`useUserQueryKey` (`src/hooks/useUserQueryKey.ts`) signature: `(baseKey: QueryKey): QueryKey` — appends the current user id.

`useRedlineTurn` (`src/pages/CollaborationToolPage/collab/useRedlineTurn.ts`) already exposes `canAct` (composed: `isParticipant ? (turnGateReady ? isMyTurn : false) : false`) and `isLocked` (`= !canAct`) — both already used elsewhere in `index.tsx` (e.g. `redlineTurn.isLocked` at line ~545).

`useUserRole` (`src/hooks/useUserRole.ts`) returns `{ userRole, isCompanyAdmin, ... }` — `userRole` is the discriminant to key role-specific UI off of.

`dashboardConfigs` (`src/config/dashboardConfig.ts`) exports `Record<UserRole, DashboardConfig>`; `dashboardConfigs.contract_manager.rows` is `[{ type: "activity", properties: [{ id: "my-actions", ... }, { id: "general-updates", ... }] }]` — the canonical contract-shaped activity-row template already reused elsewhere via `buildContractTotalStats(dashboardConfigs.contract_manager.stats, cards)` (see `RoleBasedDashboard/index.tsx` `cmTotalStats`, line ~387-394). Company Admin's own `dashboardConfig.rows` (companyAdminConfig) has NO `"activity"`-type row — its "General Updates" lives inside a `"mixed"`-type row with no `"my-actions"` sibling at all — so it cannot supply contract-shaped activity rows itself.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Vendor Personnel save invalidates parent contract/MSA detail query (#253)</name>
  <files>src/pages/ContractManagementPage/layouts/VendorPersonnelTabContent.tsx, src/pages/ContractManagementPage/ContractDetailPage.tsx, src/pages/MsaPage/MsaDetailPage.tsx, src/pages/ContractManagementPage/__tests__/vendor-personnel-tab.test.tsx</files>
  <behavior>
    - When `VendorPersonnelTabContent` is rendered with an `invalidateQueryKey` prop and a personnel save succeeds, `queryClient.invalidateQueries` is called with `{ queryKey: invalidateQueryKey }` (in addition to the existing call with the tab's own `vendor-personnel` `queryKey`).
    - When `invalidateQueryKey` is omitted (existing test suite's default `renderTab()` calls), save still succeeds and only the tab's own `queryKey` is invalidated — no crash, no extra call.
  </behavior>
  <action>
    1. In `VendorPersonnelTabContent.tsx`: add `invalidateQueryKey?: unknown[];` to `Props` (below `contractType`), destructure it in the component signature. In `saveMutation`'s `onSuccess` (currently `onSuccess: async () => { await qc.invalidateQueries({ queryKey }); }`), add a second invalidation guarded by presence: `if (invalidateQueryKey) { await qc.invalidateQueries({ queryKey: invalidateQueryKey }); }` alongside the existing one. This fixes #253 — the parent contract/MSA detail view goes stale after add/edit/remove because only the tab's own query was invalidated.
    2. Wire it from `ContractDetailPage.tsx` at the `<VendorPersonnelTabContent>` call site (~line 721-727): add `invalidateQueryKey={[...queryKey]}` where `queryKey` is the existing local var (`useUserQueryKey(["contract-manager-contracts", id])`, line ~201) — follow the exact existing precedent already used one call site above for `AssignProjectManagerDialog` (`invalidateQueryKey={[...queryKey]}` at line 644).
    3. Wire it from `MsaPage/MsaDetailPage.tsx` at the `<VendorPersonnelTabContent>` call site (~line 966-972): add `invalidateQueryKey={[...queryKey]}` where `queryKey` is `useUserQueryKey(["msa-contracts-detail", id])` (line ~389).
    4. Add the behavior test cases above as a new test in `vendor-personnel-tab.test.tsx`, using a directly-constructed `QueryClient` + `vi.spyOn(queryClient, "invalidateQueries")` (not the existing `renderTab()` helper, since it needs access to the client before render) so you can assert the spy was called with `expect.objectContaining({ queryKey: [...] })` for the passed `invalidateQueryKey` after triggering a successful add-personnel save.
  </action>
  <verify>
    <automated>npx vitest run src/pages/ContractManagementPage/__tests__/vendor-personnel-tab.test.tsx && npx tsc -b</automated>
  </verify>
  <done>All existing vendor-personnel-tab tests plus the new invalidateQueryKey test pass; both ContractDetailPage.tsx and MsaDetailPage.tsx pass invalidateQueryKey to VendorPersonnelTabContent; tsc reports no new errors.</done>
</task>

<task type="auto">
  <name>Task 2: Redline auto-run turn guard (#256) + Admin delete endpoint fix (#258)</name>
  <files>src/pages/CollaborationToolPage/index.tsx, src/pages/AdminManagementPage/index.tsx</files>
  <action>
    1. In `CollaborationToolPage/index.tsx`, the "Auto-run the first time the user opens the Redline tab" effect (~lines 520-525, currently `if (activeTab !== "redline") return; if (aiHasRun) return; void runAiSuggestions();`): add `if (!redlineTurn.canAct) return;` after the `aiHasRun` check and before calling `runAiSuggestions()`. Add `redlineTurn.canAct` to the effect's dependency array (`[activeTab, aiHasRun, runAiSuggestions, redlineTurn.canAct]`). This fixes #256 — AI redline was auto-generating regardless of whose negotiation turn it is; it must still auto-generate normally when it IS the user's turn.
    2. In `AdminManagementPage/index.tsx` line ~299, the `deleteAdmin` mutation's `mutationFn` currently calls `deleteRequest({ url: \`/admins/${adminId}\` })`. Change the URL to `` `/delete/${adminId}` `` — the correct endpoint, matching the working call already used by `AdminDetailsSheet.tsx:91` (`deleteRequest({ url: \`/delete/${adminId}\` })`). This fixes #258 — the row-level Delete action was calling the wrong endpoint.
  </action>
  <verify>
    <automated>npx tsc -b && grep -n "redlineTurn.canAct" src/pages/CollaborationToolPage/index.tsx | grep -q . && grep -n "url: \`/delete/\${adminId}\`" src/pages/AdminManagementPage/index.tsx | grep -q .</automated>
  </verify>
  <done>The Redline-tab auto-run effect returns early when redlineTurn.canAct is false and still runs when it's true; the admin list row Delete action calls /delete/{adminId}, matching AdminDetailsSheet's working call; tsc reports no new errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Dashboard chart fixes — real percentage, drop stray bar series, left-align legend (#259/#260, #261, #264)</name>
  <files>src/lib/dashboardDataTransformer.ts, src/components/layouts/RoleBasedDashboard/components/ChartCard.tsx, src/config/dashboardConfig.ts, src/lib/__tests__/dashboardDataTransformer.test.ts</files>
  <behavior>
    - `DashboardDataTransformer.transformSubDistribution({ totalActive: 30, distribution: [{ plan: "Basic", count: 10 }, { plan: "Pro", count: 20 }] })` returns `[{ name: "Basic", value: 10, percentage: 33, ... }, { name: "Pro", value: 20, percentage: 67, ... }]` (percentage = `Math.round(count/total*100)`, colors applied via existing `applyConsistentColors`).
    - `DashboardDataTransformer.transformSubDistribution(undefined)` still returns the 3-entry zeroed fallback, now with `percentage: 0` on each entry.
  </behavior>
  <action>
    1. In `dashboardDataTransformer.ts`, `transformSubDistribution` (~lines 976-996): compute `const total = data.distribution.reduce((sum, item) => sum + (item.count || 0), 0);` and change the `chartData` map to `{ name: item.plan, value: item.count, percentage: total > 0 ? Math.round((item.count / total) * 100) : 0 }` — matching the existing pattern already used by `transformCompanyRoleDistribution` (~lines 1460-1488). Add `percentage: 0` to each of the 3 empty-state fallback literal objects for shape consistency. This fixes #259/#260 — the legend was printing the raw count suffixed with "%".
    2. In `ChartCard.tsx`, the donut chart's `Legend` `formatter` (~lines 318-330, currently `typeof item.value === "number" ? item.value.toFixed(0) : 0`): replace with the same percentage-aware expression already used by the pie legend formatter a few lines up (~lines 248-250): `typeof (item.percentage ?? item.value) === "number" ? Number(item.percentage ?? item.value).toFixed(0) : 0`. This makes the donut legend prefer the real percentage when present, falling back to `value` for donut charts that don't carry a percentage field (no regression for those).
    3. In `ChartCard.tsx`, `getDataKeys` (~lines 484-501): change `return allKeys.filter((key) => key !== "name");` to `return allKeys.filter((key) => key !== "name" && key !== "percentage");` — `percentage` is a display-only companion field (already present on `transformCompanyRoleDistribution`'s output, which feeds the "Role Distribution" bar chart) and must never be rendered as its own plotted bar series. This fixes #261 — the unwanted second, unlabeled, randomly-colored "percentage" bar.
    4. In `dashboardConfig.ts`, the Company Admin `"solicitation-status"` chart config (~lines 342-350, `id: "solicitation-status", title: "Solicitation Status", type: "pie"`): add `legendPosition: { horizontal: "left" },` to the config object. This fixes #264 — the legend was center-aligned by default.
    5. Add the behavior test cases above to `dashboardDataTransformer.test.ts` as a new `describe("transformSubDistribution", ...)` block.
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/dashboardDataTransformer.test.ts && npx tsc -b</automated>
  </verify>
  <done>New transformSubDistribution tests pass alongside the existing test; donut legend renders the real percentage; Role Distribution bar chart renders only the value series (grep confirms `key !== "percentage"` in ChartCard.tsx); company_admin's Solicitation Status chart config has legendPosition.horizontal === "left"; tsc reports no new errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Company Admin Contracts tab shows real contract data, not solicitation data (#262/#263)</name>
  <files>src/hooks/useDashboardData.ts, src/components/layouts/RoleBasedDashboard/index.tsx, src/hooks/__tests__/useDashboardData.test.tsx</files>
  <behavior>
    - `useDashboardData("company_admin")` requests AI insights from `/contract/manager/contracts/dashboard/ai-insights` with `type: "Contract"` param — mirroring the existing "procurement"/"approver" tests in this same file.
  </behavior>
  <action>
    1. In `useDashboardData.ts`, `isContractDashboardRole` (~lines 371-374, currently `userRole === "contract_manager" || userRole === "approver" || userRole === "procurement"`): add `|| userRole === "company_admin"`. This enables all `contractManagerXXX` queries (cards, cycle-time, invoice-status, risk, etc. — currently `enabled: isContractDashboardRole` at ~14 call sites) for company_admin; `contractDashboardBasePath` stays `/contract/manager/contracts/dashboard` for company_admin (only `approver` gets the different `/contract/approver/...` path). This fixes #262.
    2. In `RoleBasedDashboard/index.tsx`, add a new `companyAdminContractsRows` `useMemo` (placed near `procurementContractsRows`, ~line 958) that builds contract-shaped activity rows from `dashboardConfigs.contract_manager.rows` (filtered to `row.type === "activity"`), with `contractMyActions`/`contractGeneralUpdates` populated via `DashboardDataTransformer.transformContractManagerDashboardActivity(contractManagerActionLogs)` / `(contractManagerGeneralUpdates)` — same data source and transform as `procurementContractsRows`, but sourced from `dashboardConfigs.contract_manager.rows` instead of `enhancedDashboardConfig.rows` (company_admin's own base config has no `"activity"`-type row to reuse — see `<interfaces>`). Dependency array: `[contractManagerActionLogs, contractManagerGeneralUpdates]`.
    3. In the company_admin `<ContractsTabView>` branch (~lines 1113-1150), change `stats={enhancedDashboardConfig.stats}` to `stats={cmTotalStats}` and `rows={enhancedDashboardConfig.rows}` to `rows={companyAdminContractsRows}` — mirroring procurement's own branch (`stats={cmTotalStats}` / `rows={procurementContractsRows}`, ~lines 1068-1105) instead of reusing the solicitation-shaped `companyAdminConfig`. This fixes #263.
    4. Add the behavior test above to `useDashboardData.test.tsx`, mirroring the existing "requests AI insights for procurement" test but for `"company_admin"`.
  </action>
  <verify>
    <automated>npx vitest run src/hooks/__tests__/useDashboardData.test.tsx && npx tsc -b</automated>
  </verify>
  <done>New company_admin AI-insights test passes alongside existing procurement/approver tests; company_admin's Contracts tab passes cmTotalStats/companyAdminContractsRows to ContractsTabView instead of the solicitation-shaped enhancedDashboardConfig; tsc reports no new errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Company Admin nav reorder/prune (#265/#266) + role-aware AI chat prompts (#269)</name>
  <files>src/lib/navigation.ts, src/components/layouts/AIChatWidget/index.tsx, src/lib/__tests__/navigation.test.ts</files>
  <behavior>
    - `getNavigationForRole("company_admin", "/dashboard", allModules)` returns items where the item immediately after "Solicitation Management" is titled "Evaluation Management" (not "Evaluation"), and no returned item is titled "Contract Management".
  </behavior>
  <action>
    1. In `navigation.ts`'s `company_admin` array (~lines 314-381): move the `isModuleEnabled(modules?.evaluationsManagement) ? {...} : undefined` "Evaluation" entry (currently ~367-374, last before "Profile") to directly after the "Solicitation Management" ternary (currently ends ~line 322, right before "Contract Management" begins at ~323) — i.e. it becomes the second entry in the array. Rename its `title` from `"Evaluation"` to `"Evaluation Management"`. Per #265.
    2. Remove the entire "Contract Management" ternary block (~lines 323-346, including its nested `children` array for Contracts/MSA) from the `company_admin` array entirely. Per #266. Resulting order: Solicitation Management -> Evaluation Management -> Business Divisions -> User Management -> Vendor Management -> Profile.
    3. Add the behavior test above to `navigation.test.ts`.
    4. In `AIChatWidget/index.tsx`: import `useUserRole` from `@/hooks/useUserRole`. Add a new module-level `COMPANY_ADMIN_SUGGESTED_PROMPTS` array (e.g. `"Show subscription distribution across plans"`, `"How many active vendors are on the platform?"`, `"What's the current company-wide role distribution?"`) alongside the existing `SUGGESTED_PROMPTS`. Inside the `AIChatWidget` component, call `const { userRole } = useUserRole();` and derive `const suggestedPrompts = userRole === "company_admin" ? COMPANY_ADMIN_SUGGESTED_PROMPTS : SUGGESTED_PROMPTS;`. Replace the `SUGGESTED_PROMPTS.map(...)` render call (~line 808) with `suggestedPrompts.map(...)`. Leave `welcomeMessage` untouched — it's sourced from `App.tsx`'s `chatWelcomeMessage` (name-based greeting, no per-contract language), so it does not need role-awareness. Per #269.
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/navigation.test.ts && npx tsc -b && grep -n "COMPANY_ADMIN_SUGGESTED_PROMPTS" src/components/layouts/AIChatWidget/index.tsx | grep -q .</automated>
  </verify>
  <done>New navigation test passes alongside existing role-access tests; company_admin nav array has Evaluation Management right after Solicitation Management with no Contract Management entry; AIChatWidget renders a distinct, company-wide-appropriate prompt set for company_admin via useUserRole(); tsc reports no new errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (company_admin) -> BE contract-manager-scoped dashboard endpoints | Task 4 makes company_admin's FE start requesting `/contract/manager/contracts/dashboard/*` endpoints it previously never called. |
| Browser (admin) -> BE admin delete endpoint | Task 2 changes which URL the row-level delete action targets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-260724t75-01 | Elevation of Privilege | `useDashboardData.ts` `isContractDashboardRole` now includes `company_admin` | accept | FE only stops suppressing the request — server-side authorization for `/contract/manager/contracts/dashboard/*` is enforced by the BE per role, unchanged by this fix. If the BE doesn't already permit company_admin (the company's top-level admin) to read this data, it will 403 server-side; no FE change can bypass that. |
| T-260724t75-02 | Tampering | `AdminManagementPage/index.tsx` delete URL corrected to `/delete/{adminId}` | accept | This only corrects which URL string is called; ownership/authorization checks for the delete happen server-side at that endpoint, identical to the already-shipped `AdminDetailsSheet.tsx` delete call this fix now matches. |
</threat_model>

<verification>
- `npx tsc -b` reports no new type errors after all 5 tasks.
- `npx vitest run src/pages/ContractManagementPage/__tests__/vendor-personnel-tab.test.tsx src/lib/__tests__/dashboardDataTransformer.test.ts src/hooks/__tests__/useDashboardData.test.tsx src/lib/__tests__/navigation.test.ts` passes (scoped — do not run a bare `npx vitest run` across the whole suite; memory: vitest scans stale `.claude/worktrees` and can produce unrelated failures).
- Every one of the 9 QA items (#253, #256, #258, #259/#260, #261, #262/#263, #264, #265/#266, #269) maps to exactly one task above.
</verification>

<success_criteria>
- Vendor personnel add/edit/remove refreshes the parent contract/MSA detail view without a manual reload.
- AI redline no longer auto-generates on the opposing side's turn; still auto-generates on the user's own turn.
- Admin list-row Delete calls the correct endpoint and succeeds.
- Subscription Distribution legend shows a real percentage; Role Distribution chart shows one bar series per role; Solicitation Status legend is left-aligned.
- Company Admin's Contracts Overview/Analytics tab shows contract data, not solicitation data.
- Company Admin nav shows Evaluation Management after Solicitation Management with no Contract Management entry.
- AI chat suggested prompts are company-admin-appropriate for that role.
</success_criteria>

<output>
After completion, create `.planning/quick/260724-t75-fix-9-confirmed-qa-bugs-vendor-personnel/260724-t75-SUMMARY.md`
</output>
