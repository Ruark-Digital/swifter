---
phase: 260723-9dx-make-step-2-personnel-read-only-on-edit-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/ContractManagementPage/components/Step2ContractTeam.tsx
  - src/pages/ContractManagementPage/components/EditContract.tsx
  - src/pages/MsaPage/components/Step2ContractTeam.tsx
  - src/pages/MsaPage/layouts/CreateMSADialog.tsx
autonomous: true
requirements: [QA-260723-9dx]
must_haves:
  truths:
    - "On the Contract EDIT form, Step 2 personnel field is no longer an editable TextTagInput — it shows the hydrated personnel as static text (or a clear note) plus guidance to use the Vendor Personnel tab"
    - "On the MSA EDIT form, Step 2 personnel field is likewise read-only"
    - "On Contract CREATE and MSA CREATE, Step 2 personnel field is unchanged — still an editable TextTagInput submitted at creation"
  artifacts:
    - path: "src/pages/ContractManagementPage/components/Step2ContractTeam.tsx"
      provides: "personnelReadOnly prop gating editable vs read-only personnel block"
    - path: "src/pages/MsaPage/components/Step2ContractTeam.tsx"
      provides: "personnelReadOnly prop gating editable vs read-only personnel block"
  key_links:
    - from: "src/pages/ContractManagementPage/components/EditContract.tsx"
      to: "src/pages/ContractManagementPage/components/Step2ContractTeam.tsx"
      via: "personnelReadOnly={true} prop"
      pattern: "personnelReadOnly"
    - from: "src/pages/MsaPage/layouts/CreateMSADialog.tsx"
      to: "src/pages/MsaPage/components/Step2ContractTeam.tsx"
      via: "personnelReadOnly={isEditing} prop"
      pattern: "personnelReadOnly=\\{isEditing\\}"
---

<objective>
Stop the Step 2 "Vendor/Contractor Key Personnel" field from rendering as an editable input on EDIT forms (Contract EditContract.tsx and MSA CreateMSADialog.tsx in edit mode), since personnel is no longer saved from the edit PUT (it's owned by the dedicated Vendor Personnel tab per commits 6cd5433f2 / e79037cfd / d23fcfcf9). Replace it with a read-only informational block on edit; leave CREATE mode fully editable and unchanged.

Purpose: Remove the confusing UX of an editable field whose edits are silently discarded on save.
Output: `personnelReadOnly` prop added to both Step2ContractTeam components, wired true only on edit paths.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md

<interfaces>
<!-- Contract Step2ContractTeam.tsx — current Props and personnel Forger -->
Current Props type (src/pages/ContractManagementPage/components/Step2ContractTeam.tsx:18-21):
```
type Props = {
  setValue?: UseFormSetValue<CreateContractFormData>;
  projectManagerFallbackLabel?: string;
};
```

Current personnel Forger (same file, ~line 260-266):
```
<Forger
  name="personnel"
  label="Vendor/Contractor Key Personnel. (Multi-Select)"
  component={TextTagInput}
  enableDetailsPopover
  helperText="Add vendor's/contractor's key personnel with email address, name"
/>
```

The form's `personnel` field value (hydrated in EditContract.tsx buildDefaultValues, ~line 513-526) is an array shaped:
```
Array<{ id?: string; text?: string; meta?: { email?: string; role?: string; phone?: string } }>
```
`text` is the display name/email fallback; `meta.email` is the email when present.

<!-- MSA Step2ContractTeam.tsx — current Props and personnel Forger -->
Current Props type (src/pages/MsaPage/components/Step2ContractTeam.tsx:16-21):
```
type Props = {
  defaultPmLabel?: string;
};
```

Current personnel Forger (same file, ~line 210-216) is identical shape/name to the contract twin.

MSA form's `personnel` field is hydrated the same way in CreateMSADialog.tsx (~line 322-334), same
`{ id, text, meta: { email, role, phone } }` shape.

<!-- Render call sites -->
EditContract.tsx renders Step2 at ~line 1177-1184 (EDIT-only component, no create path in this file):
```
{step === 2 && (
  <Step2ContractTeam
    projectManagerFallbackLabel={
      (contractRes?.data?.data as any)?.projectManager?.user?.name
    }
  />
)}
```

CreateContractSheet.tsx renders Step2 at ~line 1219 (CREATE-only, must stay untouched):
```
{step === 2 && <Step2ContractTeam setValue={setValue} />}
```

CreateMSADialog.tsx renders Step2 at ~line 1130 (handles BOTH create and edit via `isEditing`,
defined at line 217 as `const isEditing = Boolean(editingMsaId);`):
```
{step === 2 && <Step2ContractTeam defaultPmLabel={defaultPmLabel} />}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add personnelReadOnly to Contract Step2ContractTeam and wire EditContract</name>
  <files>src/pages/ContractManagementPage/components/Step2ContractTeam.tsx, src/pages/ContractManagementPage/components/EditContract.tsx</files>
  <action>
In src/pages/ContractManagementPage/components/Step2ContractTeam.tsx: add `personnelReadOnly?: boolean;` to the `Props` type (default to `false` via destructuring, e.g. `personnelReadOnly = false`). Read the current `personnel` field value via `useWatch({ control: formContext.control, name: "personnel" })` typed as `Array<{ id?: string; text?: string; meta?: { email?: string } }> | undefined` (reuse the existing `formContext`/`useWatch` import already in this file — do not add a new form-context instance). Replace the single `<Forger name="personnel" .../>` block with a conditional: when `personnelReadOnly` is true, render a static block instead — a label matching the existing field's visual weight (e.g. a `<div>` with a `<p>`/`<label>` styled like other field labels in this file, e.g. `text-sm font-medium`), followed by either a list of the watched personnel entries (`text` plus `meta.email` in parentheses if present) or, if the array is empty, a muted "No personnel on record." line, and always a trailing helper note "Manage vendor personnel from the Vendor Personnel tab." styled like the existing `helperText` props elsewhere in this file (muted, small text). When `personnelReadOnly` is false (default), keep the exact existing `<Forger name="personnel" .../>` block unchanged. Leave `internalTeam` and every other Forger in this file untouched.

In src/pages/ContractManagementPage/components/EditContract.tsx: at the Step2ContractTeam render call (~line 1178), add `personnelReadOnly` as a prop set to `true` (this file only renders the edit flow, so it is unconditionally true here).
  </action>
  <verify>
    <automated>npx tsc -b</automated>
  </verify>
  <done>Step2ContractTeam.tsx compiles with the new prop; when personnelReadOnly is true it renders static personnel text + note instead of TextTagInput; EditContract.tsx passes personnelReadOnly={true}; CreateContractSheet.tsx is untouched and still renders the editable Forger (personnelReadOnly defaults false there).</done>
</task>

<task type="auto">
  <name>Task 2: Add personnelReadOnly to MSA Step2ContractTeam and wire CreateMSADialog</name>
  <files>src/pages/MsaPage/components/Step2ContractTeam.tsx, src/pages/MsaPage/layouts/CreateMSADialog.tsx</files>
  <action>
Apply the same pattern as Task 1 to the MSA twin. In src/pages/MsaPage/components/Step2ContractTeam.tsx: add `personnelReadOnly?: boolean;` to `Props` (default `false`), watch the `personnel` field via the existing `formContext`/`useWatch` (already imported in this file), and conditionally render the same static read-only block (personnel list with `text` + `meta.email`, or "No personnel on record.", plus the "Manage vendor personnel from the Vendor Personnel tab." note) in place of the `<Forger name="personnel" .../>` block when `personnelReadOnly` is true. Match the styling used in the contract twin from Task 1 for consistency. Leave `internalTeam` and all other Forgers untouched.

In src/pages/MsaPage/layouts/CreateMSADialog.tsx: at the Step2ContractTeam render call (~line 1130), add `personnelReadOnly={isEditing}` — this file handles both create (isEditing=false, stays editable) and edit (isEditing=true, becomes read-only) in one component.
  </action>
  <verify>
    <automated>npx tsc -b</automated>
  </verify>
  <done>MSA Step2ContractTeam.tsx compiles with the new prop and mirrors the contract twin's read-only rendering; CreateMSADialog.tsx passes personnelReadOnly={isEditing} so MSA create stays editable and MSA edit becomes read-only.</done>
</task>

</tasks>

<verification>
Full type-check across both touched component pairs: `npx tsc -b` must complete with zero errors. Manually confirm by reading the diff that CreateContractSheet.tsx and the MSA create path receive no `personnelReadOnly=true` (only EditContract.tsx unconditionally, and CreateMSADialog.tsx conditionally via `isEditing`).
</verification>

<success_criteria>
- Contract EDIT (EditContract.tsx): Step 2 personnel is a read-only block, not TextTagInput.
- Contract CREATE (CreateContractSheet.tsx): Step 2 personnel is unchanged, still editable TextTagInput.
- MSA EDIT (CreateMSADialog.tsx with isEditing=true): Step 2 personnel is a read-only block.
- MSA CREATE (CreateMSADialog.tsx with isEditing=false): Step 2 personnel is unchanged, still editable TextTagInput.
- `npx tsc -b` passes clean.
</success_criteria>

<output>
After completion, create `.planning/quick/260723-9dx-make-step-2-personnel-read-only-on-edit-/260723-9dx-SUMMARY.md`
</output>
